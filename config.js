const port = 8081; // 本地调试端口号
const baseRouter = '/shop'; // 默认路由地址
const sysName = '中台系统'; // 系统名称
const clientId = 3; // 每一个系统所对应的id 获取系统菜单时用

module.exports = {
  port,
  baseRouter,
  sysName,
  clientId
}
